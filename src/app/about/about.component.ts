import { Component } from '@angular/core';
//expression(interpolation),property, event, class ,style, two waybinding
//Directives ngClass,ngStyle,ngModel,ngIf,ngFor
@Component({
  selector: 'app-about',
  template: `<h4>About Page</h4>`

})
export class AboutComponent { }
